type Course {
	_id: ID!
	title: String
	teacher: String
	description: String
	topic: String
}

type Query {
  "return all courses"
  getCourses: [Course]
  "return a course"
  getCourse(id: ID!): Course
}